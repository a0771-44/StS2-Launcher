pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

include ':assetPackInstallTime'

// ❗ 중요: plugin versions는 여기서 제거해야 안전함
